*{
  margin: 0px;
  padding: 0px;
  box-sizing: border-box;
  outline: none;
}

ul,li{
  list-style: none;
}

a{
  text-decoration: none;
  color: $dark;

  &:active{
    color: $dark;
  }
}

button{
  cursor: pointer;
}
